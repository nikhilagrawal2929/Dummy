createElement(doc, customer, "TEMPLATE_COMMON_NAME", xlsData.getTemplateCommonName());
            createElement(doc, customer, "Brand", xlsData.getBrand());
            createElement(doc, customer, "MAILING_REF", xlsData.getMailingRef());
            createElement(doc, customer, "REF", xlsData.getRef());
            createElement(doc, customer, "Title", xlsData.getTitle());
            createElement(doc, customer, "First_Names", xlsData.getFirstNames());
            createElement(doc, customer, "Surname", xlsData.getSurname());
            createElement(doc, customer, "Address_Line_1", xlsData.getAddressLine1());
            createElement(doc, customer, "Address_Line_2", xlsData.getAddressLine2());
            createElement(doc, customer, "Address_Line_3", xlsData.getAddressLine3());
            createElement(doc, customer, "Address_Line_4", xlsData.getAddressLine4());
            createElement(doc, customer, "Address_Line_5", xlsData.getAddressLine5());
            createElement(doc, customer, "Postcode", xlsData.getPostcode());
            createElement(doc, customer, "Country_Name", xlsData.getCountryName());
            createElement(doc, customer, "CIN", xlsData.getCin());
            createElement(doc, customer, "Provider_Name", xlsData.getProviderName());
            createElement(doc, customer, "Product_Name", xlsData.getProductName());
            createElement(doc, customer, "Date_Product_Was_Sold", xlsData.getDateProductWasSold());
            createElement(doc, customer, "Date_Product_Was_Bought", xlsData.getDateProductWasBought());
            createElement(doc, customer, "Total_Payment_Value", xlsData.getTotalPaymentValue());
            createElement(doc, customer, "Payment_Lieu", xlsData.getPaymentLieu());
            createElement(doc, customer, "Tax_Deducted", xlsData.getTaxDeducted());
            createElement(doc, customer, "Account_Ending", xlsData.getAccountEnding());
            createElement(doc, customer, "Calculation_Date", xlsData.getCalculationDate());
            createElement(doc, customer, "Initial_Investment", xlsData.getInitialInvestment());
            createElement(doc, customer, "Current_Value", xlsData.getCurrentValue());
            createElement(doc, customer, "Nominal_Value", xlsData.getNominalValue());
            createElement(doc, customer, "Difference", xlsData.getDifference());
            createElement(doc, customer, "Letter_Post_Date", xlsData.getLetterPostDate());
            createElement(doc, customer, "PRODUCT_CLOSED_DATE", xlsData.getProductClosedDate());
            createElement(doc, customer, "TOTAL_PAYMENT", xlsData.getTotalPayment());
            createElement(doc, customer, "WITHDRAWAL_VALUE", xlsData.getWithdrawalValue());
            createElement(doc, customer, "RATIONALE", xlsData.getRationale());
            createElement(doc, customer, "ME_SUITABILITY_FAILURE_REASON", xlsData.getMeSuitabilityFailureReason());
            createElement(doc, customer, "BENCHMARK", xlsData.getBenchmark());
            createElement(doc, customer, "CLOSED_VALUE", xlsData.getClosedValue());
            createElement(doc, customer, "PRODUCT_PROVIDER", xlsData.getProductProvider());
            createElement(doc, customer, "ALTERNATIVE_PRODUCT_OR_FUND_COMPARISON", xlsData.getAlternativeProductOrFundComparison());
            createElement(doc, customer, "AMOUNT_ABOVE_BENCHMARK", xlsData.getAmountAboveBenchmark());
            createElement(doc, customer, "SUITABILITY", xlsData.getSuitability());
            createElement(doc, customer, "Date_Of_Letter", xlsData.getDateOfLetter());
            createElement(doc, customer, "DATE_OF_LETTER_90_DAYS", xlsData.getDateOfLetter90Days());
            createElement(doc, customer, "SIGNATURE", xlsData.getSignature());
            createElement(doc, customer, "SIGNED_DATE", xlsData.getSignedDate());
            createElement(doc, customer, "DATE_OF_LAST_LETTER_SENT", xlsData.getDateOfLastLetterSent());
